# Python
.venv*/
__pycache__/
*.pyc
.pytest_cache/

# Private skin — never commit or publish
web/skins/official/

# Local reference material — never commit or publish
*.pptx
*.pdf

# Local-only real-meter verification checklist — never commit
docs/meter-verification.md
