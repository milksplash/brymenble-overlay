# Python
.venv*/
__pycache__/
*.pyc
.pytest_cache/

# Private skin — never commit or publish
web/skins/official/

# Local meter verification forms — never commit or publish
docs/meter-verification.*

# Local reference material — never commit or publish
*.pptx
*.pdf
